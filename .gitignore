# Byte-compiled / build / cache
__pycache__/
*.py[cod]
*$py.class

# Diretórios de ambiente virtual
venv/              # se seu venv se chamar "venv"
.env/              # outro nome comum
.env.bak           # backup, caso exista
.venv/             # outro formato de venv

# PyTest cache
.pytest_cache/
.cache/
.coverage          # arquivo de cobertura (caso gerado)
htmlcov/           # relatório HTML de cobertura, se for gerado

# Configurações de IDE
.idea/             # PyCharm
.vscode/           # VSCode
*.suo
*.user
*.userosscache
*.sln
*.dbmdl

# Arquivos de histórico / gerados em tempo de execução
historico.json     # se você usa History e não quer versionar o arquivo de histórico
*.log              # logs gerais (caso você grave logs)
*.sqlite3          # se alguma vez usar banco SQLite local

# Dependências empacotadas
dist/
build/
*.egg-info/
.eggs/

# Arquivos temporários do sistema
.DS_Store          # macOS
Thumbs.db          # Windows

# Arquivos de exportação do PyInstaller
# (caso você gerasse executáveis .exe com PyInstaller)
*.manifest
*.spec

# Se tiver notebooks (Jupyter)
.ipynb_checkpoints/

# Arquivos de cache extra (se houver)
__MACOSX/